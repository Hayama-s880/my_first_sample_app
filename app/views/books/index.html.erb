<h1>一覧画面</h1>
<%= link_to('記事作成','books/new')%>
<% @books.each do |f| %>
<p>
    <%= f.id %>
    <%= f.title %>
    <%= f.author %>
    <%= f.pages %>
    <%= link_to('削除',"/books/delete/#{f.id}",method: :post) %>
    
</p>
<% end %>
