<h1>一覧画面</h1>
<%= link_to('記事作成','books/new')%>
<% @books.each do |f| %>
<p>
    <%= f.id %>
    <%= f.title %>
    <%= f.author %>
    <%= f.pages %>
    <%= link_to('削除',"/books/delete/#{f.id}",method: :post) %>
    <%= link_to('編集',"/books/edit/#{f.id}")%>
    
</p>
<% end %>
